@model ModLearn.Models.Team

@{
    ViewBag.Title = "TeamOf";
}

<h3>@Model.Name players</h3>
<br />
<dd>
    <ul>
        @foreach (var player in Model.Players)
        {
            <li>@player.Name</li>
        }
    </ul>
</dd>